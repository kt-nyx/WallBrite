<Window x:Class="WallBrite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel x:Name="MainPanel">
        <Border x:Name="SidePanel" Width="105" Background="GhostWhite" BorderBrush="Black" BorderThickness="0 0 2 0" Padding="5 0">
            <StackPanel DockPanel.Dock="Left">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Margin" Value="0, 2"/>
                    </Style>
                </StackPanel.Resources>
                <TextBlock FontWeight="Bold" FontSize="14" TextWrapping="WrapWithOverflow" TextAlignment="Center" Margin="0 5">Library Controls</TextBlock>

                <StackPanel>
                    <Button Command="{Binding AddFilesCommand}">Add Files</Button>
                    <Button Command="{Binding AddFolderCommand}">Add Folder</Button>
                </StackPanel>

                <StackPanel Margin="0 5">
                    <TextBlock>Sort By:</TextBlock>
                    <ComboBox x:Name="SortTypeBox" SelectionChanged="SortTypeChanged" SelectedValuePath="Content" SelectedIndex="1">
                        <ComboBoxItem>Brightness</ComboBoxItem>
                        <ComboBoxItem>Date Added</ComboBoxItem>
                        <ComboBoxItem>Enabled</ComboBoxItem>
                    </ComboBox>
                    <TextBlock>Sort Direction:</TextBlock>
                    <ComboBox x:Name="SortDirectionBox" SelectionChanged="SortDirectionChanged" SelectedValuePath="Content" SelectedIndex="1">
                        <ComboBoxItem>Descending</ComboBoxItem>
                        <ComboBoxItem>Ascending</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
                <StackPanel Margin="0 10">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="0 2"/>
                            <Style.Triggers>
                                <DataTrigger 
                                        Binding ="{Binding ElementName=imageGrid, Path=SelectedItems.Count}" 
                                        Value="0">
                                    <Setter Property="Button.IsEnabled" Value="false"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock TextAlignment="Center" FontWeight="Medium">Selection:</TextBlock>
                    <Button x:Name="EnableButton" Command="{Binding EnableCommand}" CommandParameter="{Binding ElementName=imageGrid, Path=SelectedItems}">Enable</Button>
                    <Button x:Name="DisableButton" Command="{Binding DisableCommand}" CommandParameter="{Binding ElementName=imageGrid, Path=SelectedItems}">Disable</Button>
                    <Button x:Name="RemoveButton" Command="{Binding RemoveCommand}" CommandParameter="{Binding ElementName=imageGrid, Path=SelectedItems}">
                        <TextBlock TextWrapping="Wrap" TextAlignment="Center">Remove from Library</TextBlock>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border x:Name="BottomPanel" DockPanel.Dock="Bottom" Background="GhostWhite" BorderBrush="Black" BorderThickness="0 2 0 0" Padding="5 5">
            <StackPanel>
                <TextBlock FontWeight="Bold" FontSize="14" TextAlignment="Center" Margin="0 0 0 5">Automation Settings</TextBlock>
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0 0 5 0">Update Every:</TextBlock>
                            <xctk:IntegerUpDown Maximum="23" Minimum="0" Value="{Binding UpdateIntervalHours}"></xctk:IntegerUpDown>
                            <TextBlock Margin="2 0 5 0" VerticalAlignment="Bottom">hrs</TextBlock>
                            <xctk:IntegerUpDown Maximum="59" Minimum="0" Value="{Binding UpdateIntervalMins}"></xctk:IntegerUpDown>
                            <TextBlock Margin="2 0 0 0" VerticalAlignment="Bottom">mins</TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Margin="0 0 20 0">
                                <TextBlock Margin="0 3">Brightest Time of Day</TextBlock>
                                <xctk:TimePicker x:Name="brightestPicker" Value="{Binding BrightestTime}" Width="120" Margin="0 0 0 3" HorizontalAlignment="Left"></xctk:TimePicker>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Margin="0 3">Darkest Time of Day</TextBlock>
                                <xctk:TimePicker x:Name="darkestPicker" Value="{Binding DarkestTime}" Margin="0 0 0 3" Width="120" HorizontalAlignment="Left"></xctk:TimePicker>
                            </StackPanel>
                        </StackPanel>
                        <Grid Width="260" HorizontalAlignment="Left">
                            <ProgressBar VerticalAlignment="Bottom" Height="25" Grid.Column="1" Minimum="0" Maximum="1" Value="{Binding Progress}" x:Name="timeProgress"></ProgressBar>
                            <TextBlock Text="{Binding ProgressReport}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                        <Image Height="100" Source="{Binding CurrentClosestImageThumb}"></Image>
                        <TextBlock Margin="0 3" HorizontalAlignment="Center">Current Wallpaper</TextBlock>
                    </StackPanel>
                </Grid>
                <Button Click="Cool1Click" Margin="0 5 0 0">Cool1</Button>
            </StackPanel>
        </Border>
        <ListView x:Name="imageGrid" ItemsSource="{Binding}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.DataContext>
                <CollectionViewSource Source="{Binding LibraryList}">
                    <CollectionViewSource.SortDescriptions>
                    </CollectionViewSource.SortDescriptions>
                </CollectionViewSource>
            </ListView.DataContext>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="imageBorder" BorderBrush="Black" BorderThickness="1" Margin="5" Background="{Binding BackgroundColor}">
                        <Border BorderBrush="Black">
                            <Image Width="200" Height="200" Source="{Binding Thumbnail}"/>
                        </Border>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
